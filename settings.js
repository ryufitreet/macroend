const SETTINGS = {
  IGNORE_FOLDERS_IN_ROOT: ["ignore", "macroend"],
  IGNORE_MODULES: ["app-template"],
  ALWAYS_IGNORE_FODLERS: ["node_modules", ".git", ".husky"],
  MICROFRONTEND_DIRECTORY: "..",
  DEFAULT_BRANCH: "development",
  RUN_WEBPACK_COMMAND: "yarn run start",
  MODULE_GROUPS: {

  },
};

export default SETTINGS;
